{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Odin",
            "type": "shell",
            "command": "odin build ${workspaceFolder}\\src -debug -out:${workspaceFolder}\\game.exe", //"command": "odin build ${workspaceFolder}\\src -out:${workspaceFolder}\\game.exe", for no -debug file -- release build.
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}